apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
nameSuffix: "-autocd"  # 修改后deploymeng中配置文件引用和ingressroute service的引用名称需要同步更新
resources:
  - ../../base/service-demo-2/
  - hpa.yaml
  - ingressroute-traefik.yaml
patchesStrategicMerge:
  - configmap-patch.yaml
  - deployment-patch.yaml
  - service-patch.yaml
